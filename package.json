{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "Application for finding weather forecasts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
  },
  "author": "Kirk Wittwer",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.16.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "got": "^11.7.0"
  },
  "devDependencies": {}
}
